# keyd re-emits the built-in keyboard as a virtual USB keyboard. Mark it internal
# so libinput pairs it with the touchpad and disable-while-typing works again.
[keyd virtual keyboard]
MatchUdevType=keyboard
MatchName=keyd virtual keyboard
AttrKeyboardIntegration=internal
